<resources>

    <style name="AppTheme" parent="BaseTheme" />

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/blue_grey_500</item>
    </style>


    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/ColorPrimary</item>
        <item name="colorPrimaryDark">@color/ColorPrimaryDark</item>
        <item name="colorAccent">@color/ColorAccent</item>
        <item name="colorControlHighlight">@color/accent_translucent</item> 
        <item name="android:textColorLink">@color/color_hyperlink</item>
        <item name="selectableItemBackground">@drawable/selectable_item_background</item>
        <!-- <item name="android:dropDownItemStyle">@style/myDropDownItemStyle</item>-->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Movie.Detail" parent="AppTheme"></style>

    <style name="Theme.Movie.List" parent="AppTheme"></style>


    <style name="RukiaFilmsActionBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- android:textColorPrimary is the  color of the title text
             in the Toolbar, in the Theme.AppCompat theme:  -->
        <item name="android:textColorPrimary">@android:color/white</item>

        <!-- android:textColorPrimaryInverse is the  color of the title
             text in the Toolbar, in the Theme.AppCompat.Light theme:
        <item name="android:textColorPrimaryInverse">@color/color_refresh_2</item>-->

        <!-- android:actionMenuTextColor is the color of the text of
              action (menu) items in the Toolbar, at least in the
              Theme.AppCompat theme.
              For some reason, they already get the textColorPrimary
              when running on API 21, but not on older versions of
              Android, so this is only necessary to support older
              Android versions.-->
        <item name="actionMenuTextColor">@android:color/white</item>
        <!-- android:textColorSecondary is the color of the menu
             overflow icon (three vertical dots)
        <item name="android:textColorSecondary">@color/color_refresh_4</item>-->

        <!-- This would set the toolbar's background color,
              but setting this also changes the popup menu's background,
              even if we define popupTheme for our <Toolbar> -->
        <!-- <item name="android:background">@color/principal</item>-->

    </style>

    <style name="Theme.ExpandedText" parent="@android:style/TextAppearance">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:lineSpacingMultiplier">0.9</item>
    </style>

    <style name="Theme.ExpandedText.Trasparent" parent="Theme.ExpandedText">
        <item name="android:textColor">#00ffffff</item>
    </style>

    <style name="SettingsTheme" parent="BaseTheme">
        <item name="colorAccent">@color/ColorSettings</item>
    </style>

    <style name="AnimationTheme" parent="BaseTheme">

        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

</resources>
